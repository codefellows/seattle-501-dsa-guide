'use strict';

// Space complexity - How is memory consumption changing as the size of the input changes


const f = (array) => { // O(1) space
  let a,b,c; // O(3) = O(1)


  let d = [1,2,3,4,5,6]; // O(6) = O(1)

  // Vinicio - e is a copy of array
  // let e = array.map(x => ); // O(N)

  return a[0];
};



f([1,3,4,5,6]);